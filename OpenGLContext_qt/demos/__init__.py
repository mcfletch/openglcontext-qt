"""Sample applications: a view inside a Qt application

:mod:`OpenGLContext_qt.demos.qt_viewer` is a window with a menu bar, a tree of
the scene and the engine's viewer beside it, run as::

    python -m OpenGLContext_qt.demos.qt_viewer model.glb

It is the same program as :mod:`OpenGLContext.demos.tk_viewer` and
:mod:`OpenGLContext.demos.wx_viewer` in the engine's own distribution, written
three times so the three can be read against one another.  What each of them
shows is in :mod:`OpenGLContext.demos`.
"""
